package utils

import configuration.ConnectionLimitations
import data_modules.Endpoint

class PointsCalculator(_connectionLimitations: ConnectionLimitations, endpoint: Endpoint, isSuccess: Boolean) {
	def calculate() : Int = {
		0
	}
}
